[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Vue Router uses history mode — redirect unknown paths to index.html so
# refreshes on /pricing, /admin, etc. don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
